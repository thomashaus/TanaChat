# This is a multipart/form-data request example
# Use curl to test:

curl -X POST http://localhost:8000/api/tana/upload \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "file=@samples/tana/imports/valid/minimal.json" \
  -F "description=Test upload of minimal Tana file"

# Or using HTTPie:
http POST localhost:8000/api/tana/upload \
  Authorization:"Bearer YOUR_JWT_TOKEN" \
  file@samples/tana/imports/valid/minimal.json \
  description="Test upload of minimal Tana file"