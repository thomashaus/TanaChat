export default {
  plugins: {
    // Enable Tailwind CSS so the @tailwind directives in src/index.css get compiled
    tailwindcss: {},
    // Autoprefixer for vendor prefixes
    autoprefixer: {},
  },
}